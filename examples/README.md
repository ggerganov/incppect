# incppect examples

The easiest way to try out the examples is:

```
cd /path/to/incppect
mkdir build && cd build
cmake ..
make

# run one of the example binaries, for example:
./examples/balls3d

```

Now open a browser and point it to port 3000 on your machine.
